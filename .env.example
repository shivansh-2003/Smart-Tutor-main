# Smart Tutor Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# LLM Configuration (Ollama)
# =============================================================================

# Base URL for Ollama API (default: http://localhost:11434)
# Make sure Ollama is running and accessible at this URL
LLM_BASE_URL=http://localhost:11434

# Complex task models (20B-32B parameters) - for complex reasoning tasks
# Used for: intent classification, math solving, mermaid generation, RAG synthesis
LLM_COMPLEX_MODEL=gpt-oss:20b
LLM_COMPLEX_FALLBACK=deepseek-r1:32b

# Simple task models (2B-3B parameters) - for lightweight tasks
# Used for: simple chat, hints, validation, query expansion
LLM_SIMPLE_MODEL=qwen2.5:3b
LLM_SIMPLE_FALLBACK=gemma2:2b

# Embedding model for RAG (Retrieval Augmented Generation)
# Used for: document embeddings, semantic search
LLM_EMBEDDING_MODEL=nomic-embed-text:latest

# =============================================================================
# Vector Database (Pinecone)
# =============================================================================

# Pinecone API Key (REQUIRED for RAG functionality)
# Get your API key from: https://app.pinecone.io/
# This is required for document indexing and retrieval
PINECONE_API_KEY=your-pinecone-api-key-here

# Pinecone Index Name (default: smart-tutor)
# The name of the Pinecone index to use for vector storage
# If the index doesn't exist, it will be created automatically
PINECONE_INDEX_NAME=smart-tutor

# =============================================================================
# Application Environment
# =============================================================================

# Environment mode: "development" or "production"
# Development: lower confidence thresholds, debug mode enabled
# Production: higher confidence thresholds, optimized settings
ENVIRONMENT=development

# =============================================================================
# Optional: Additional Configuration
# =============================================================================

# Note: Most other settings can be configured in core/config.py
# These environment variables override the defaults in the config file
